type: string
readOnly: true
description: >
  The user or application who modified the codeholder datum


  Examples:
    - `ch:3`: When a codeholder (referenced by its unique id) made the change
    - `app:2031847ee1a7bbce`: When an app (referenced by its hex-ified API key) made the change
