parameters:
  - name: code
    in: path
    required: true
    description: The code of the codeholder
    schema:
      $ref: '#/components/schemas/UEACode'

get:
  summary: Returns the latest revision of a codeholder
  responses:
    '200':
      description: OK
      content:
        application/vnd.msgpack:
          schema:
            oneOf:
              - $ref: '#/components/schemas/HumanCodeholderRevision'
              - $ref: '#/components/schemas/OrgCodeholderRevision'
    '404':
      description: Codeholder not found